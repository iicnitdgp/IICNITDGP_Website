# All configuration comes from .env.docker at the repo root (client + server
# secrets in one place). Run with:
#   docker compose --env-file .env.docker up -d --build
#
# HTTPS/reverse-proxy is handled by nginx running on the host VM itself
# (outside Docker), which proxies to these containers over localhost.

services:
  redis:
    image: redis:7-alpine
    container_name: iic-redis
    volumes:
      - redis-data:/data
    restart: unless-stopped

  server:
    build:
      context: ./server
    container_name: iic-server
    env_file:
      - ./.env.docker
    ports:
      - "127.0.0.1:${PORT:-8000}:8000"
    volumes:
      - uploads-data:/app/public/uploads
    depends_on:
      - redis
    restart: unless-stopped

  client:
    build:
      context: ./client
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_BACKEND_URI: ${VITE_BACKEND_URI}
    container_name: iic-client
    ports:
      - "127.0.0.1:5173:80"
    depends_on:
      - server
    restart: unless-stopped

volumes:
  redis-data:
  uploads-data:
